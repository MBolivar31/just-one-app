
name: just_one_viewer
description: A simple image viewer for Just One
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.2
  file_picker: ^6.1.1
  photo_view: ^0.14.0
  audioplayers: ^5.2.1

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
